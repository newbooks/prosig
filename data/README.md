# Data

Local data directories are provided for development and experiments.

- `raw/`: immutable source datasets.
- `interim/`: intermediate transformed data.
- `processed/`: final datasets used by workflows.
- `external/`: references copied or downloaded from external sources.

Large datasets and generated artifacts should stay out of git by default.
